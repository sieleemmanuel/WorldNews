<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WorldNews" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- Customize your theme here. -->
    </style>
    <style name="customTheme" parent="Theme.WorldNews">
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="MainToolbarStyle" parent="Theme.WorldNews">
        <item name="android:textColorSecondary">@color/white</item>
        <item name="toolbarStyle">@style/ToolbarStyling</item>
    </style>
    <style name="ToolbarStyling" parent="Widget.MaterialComponents.Toolbar">
        <item name="titleCentered">false</item>
        <item name="titleTextColor">@color/white</item>
    </style>
</resources>